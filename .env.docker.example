# Copie para .env na raiz ao usar docker compose
AUTH_SECRET=gere-um-segredo-longo-e-aleatorio

# Opcional: se for escanear hosts além do demo interno
# ALLOWED_SCAN_HOSTS=localhost,127.0.0.1,vulnerable-target,seu-dominio.com
